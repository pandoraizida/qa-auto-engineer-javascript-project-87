name: actions-to-do
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps: # список шагов, которые надо выполнить
      - uses: actions/checkout@v4
      - run: make install
      - run: make lint
      - run: make test